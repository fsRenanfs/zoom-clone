{
  "name": "peer-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev-windows": "set PORT=9000 && npx peerjs --port %PORT% --key peerjs --path /",
    "dev": "PORT=9000 && npm start",
    "start": "npx peerjs --port $PORT --key peerjs --path /"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",

  "dependencies": {
    "peer": "^0.6.1"
  }
}
